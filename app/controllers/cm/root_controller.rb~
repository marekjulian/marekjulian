class Cm::RootController < ApplicationController
  before_filter :login_required
  layout "cm"

  def index
    @archives = Archive.find( :all, :conditions => { :owner_id => @current_user.id} )
  end

end
